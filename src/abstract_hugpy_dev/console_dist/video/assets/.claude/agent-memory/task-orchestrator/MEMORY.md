# Memory Index

- [VI UI frontend deploy](vi-ui-frontend-deploy.md) — build/rsync/archive recipe for /video arm; gate is `npm run build` not tsc; .claude-in-assets archive landmine; ⚠2026-07-03 docroot/mount-namespace + memory-wipe anomaly note
- [VI UI workbench decomposition](vi-ui-workbench-decomposition.md) — 1 impl + 1 deploy + orchestrator-docs for tightly-coupled frontend features; + parallel recon-pair for cross-arm ports; + full-stack extension: pair every phase behind a locked JSON contract; + inference/fleet extension: recon-TRIO, held-flip contract, diagnostic phase
- [VI backend deploy gates](vi-backend-deploy-gates.md) — new video_intel job deploy/verify: VM selftest pre-gate (DB-repoint isolation, /tmp DEFAULT_ROOT ignored), one batched restart, live e2e, remote-GPU proof via daemon journal
- [VI img2img fleet release](vi-img2img-fleet-release.md) — pinned-wheel worker = fleet-release STOP boundary; HOLD the task-advertisement flip + honest availability probe so central ships dark; RUNNER_PAIRS vs HF_TASK_TO_TASKS; op now SSH-pubkey not password
