#!/usr/bin/env python3
"""Entry point for the chaos-and-learn exerciser (p1, EPOCH CLOSER).

Run:  venv/bin/python bin/hugpy-chaos --dry-run
      venv/bin/python bin/hugpy-chaos --rounds 6 --seed 1234 --max-new-tokens 24

Env:  HUGPY_BASE_URL           default http://127.0.0.1:7002
      HUGPY_OPERATOR_TOKEN     operator token for the gated /assign (else read
                               from d-env/env or --operator-token)

Records one predicted-vs-measured observation per trial to
/mnt/llm_storage/comms/chaos/observations.jsonl (+ a run manifest under runs/).
See src/abstract_hugpy_dev/chaos/SCHEMA.md for the observation contract.
"""
import os
import sys

sys.path.insert(0, os.path.join(
    os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "src"))

from abstract_hugpy_dev.chaos.runner import main

if __name__ == "__main__":
    sys.exit(main())
